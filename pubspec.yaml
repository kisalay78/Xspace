name: xspace
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.30.0
  firebase_auth: ^4.20.0
  flutter_tts: ^3.8.5
  speech_to_text: ^6.6.0
  camera: ^0.10.5+9
  http: ^1.2.0
  cloud_firestore: ^4.17.5

flutter:
  uses-material-design: true

  assets:
    - assets/questions/aptitude_questions.json
    - assets/questions/coding_questions.json

